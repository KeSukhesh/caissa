# Same Postgres as the TS monorepo (packages/db docker-compose: db=caissa, user=postgres, pw=password)
DATABASE_URL=postgresql://postgres:password@localhost:5432/caissa

# Stockfish UCI binary. macOS: `brew install stockfish`. Docker image installs it.
STOCKFISH_PATH=stockfish
